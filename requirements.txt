# Python dependencies for PINN Burgers' equation (Warm-Up 1)
#
# Install into the shared science venv:
#   source ~/.venvs/science/bin/activate
#   pip install -r requirements.txt
#
# On Aurora, `module load frameworks` provides torch and numpy.
# You only need:  pip install --user matplotlib scipy

torch>=2.0
numpy>=1.24
matplotlib>=3.7
scipy>=1.10
